# Quiz Bank

## HTML Basics

1. What does `<button>` create?
2. What is a `<div>` mostly used for?
3. What does `<input>` do?
4. What is the difference between `<h1>` and `<h2>`?

## CSS Basics

1. What is the difference between `margin` and `padding`?
2. What does `border-radius` do?
3. What does `font-family` change?
4. What does `cursor: pointer;` signal to the user?

## JavaScript Basics

1. What does `document.getElementById()` do?
2. What does `addEventListener("click", ...)` do?
3. What does `.value` mean on an input?
4. What does `textContent` change?
